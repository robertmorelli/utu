export main() I32 {
    let values: Array[I32] = Array[I32].new(8, 0);
    for (0..<values.len()) |i| {
        values[i] = i * i;
    };
    let total: I32 = 0;
    for (0..<values.len()) |i| {
        total += values[i];
    };
    total;
}
