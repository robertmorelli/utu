shimport "es" concat(str,str) str;

fun greeting(name: str) str {
    "hello " -o str.concat(_, name);
}

bench "hello-name format" |i| {
    setup {
        measure {
            greeting("utu");
        }
    }
}

export fun main() str {
    greeting("utu");
}
