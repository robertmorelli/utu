
mod Adder[T] {
    let add_: fun(T, T) T = @es/\(a,b) => a + b\/;
    fn add(t: T, c: T) T {
        add_(t, c);
    }

}

fn main() i32 {
    let y: f32 = Adder[f32].add(2.0,3.0);
    Adder[i32].add(2,3);
}