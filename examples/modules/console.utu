
mod Adder[T] {
    escape |(a,b) => a + b| add_(T, T) T;
    fun add(t: T, c: T) T {
        add_(t, c);
    }

}

export fun main() i32 {
    let y: f32 = Adder[f32].add(2.0,3.0);
    // Adder[i32].add(2,3);
}