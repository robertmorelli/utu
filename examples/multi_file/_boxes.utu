using mathops |ops| from "./_ops.utu";

mod boxes {
    struct Box:
        | value : i32

    fn Box.new(value: i32) Box {
        Box { value: value };
    }

    fn Box.score |self| () i32 {
        ops.double(self.value) + ops.triple(1);
    }
}
