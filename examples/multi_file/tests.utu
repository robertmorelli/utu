using boxes |Box| from "./_boxes.utu";

fn main() void {}

test "file imports preserve transitive module dependencies" {
    let box: Box = Box.new(4);
    assert Box.score(box) == 11;
}

test "imported promoted types support method syntax" {
    let box: Box = Box.new(5);
    assert box.score() == 13;
}
