let str_add: fun(str,str) str = @es\|(a,b) => a + b|/;
let tostr: fun(i32) str = @es\|(a) => a|/;

fn greeting(name: str) str {
    str_add("hello ", name);
}

fn five() str {
    str_add(tostr(2), tostr(3));
}

fn main() str {
    // greeting("utu");
    five();
}
