let str_concat: fun(str, str) str = @es\|(a, b) => a + b|/;

fn label() str {
    "done";
}

fn wrap(value: str) str {
    "["
    -o str_concat(&, value)
    -o str_concat(&, "]");
}

fn decorate(value: str) str {
    wrap(value);
}

fn main() str {
    decorate(label());
}
