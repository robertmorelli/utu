fun label() str {
    "done";
}

fun wrap(value: str) str {
    "[" -o str.concat(_, value) -o str.concat(_, "]");
}

fun decorate(value: str) str {
    wrap(value);
}

export fun main() str {
    decorate(label());
}
