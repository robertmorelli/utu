struct Box:
    | value : i32

fn maybe_box() ?Box {
    Box.null;
}

export lib {
    fn test_promote() i32 {
        promote maybe_box() {
            |box| => box.value,
            ~> 0,
        };
    }
}
