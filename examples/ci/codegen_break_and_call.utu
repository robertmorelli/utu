fun add_one(value: i64) i64 {
    value + 1;
}

export fun main() i64 {
    let answer: i64 = 0;

    done: {
        while () {
            answer = add_one(41);
            emit answer;
        };

        0;
    };
}
