escape "es" console_log(str) void;
escape "es" wrap(str) str;
escape |(a, b) => a === b ? 1 : 0| str_eq(str, str) bool;

fun greet(name: str) str {
    name -o wrap;
}

fun main() i64 {
    3;
}

test "imports can be called from test mode" {
    assert str_eq(greet("utu"), "[utu]");
}
