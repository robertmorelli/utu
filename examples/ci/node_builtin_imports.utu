import extern "node:fs" readFileSync(str, str) str

fn readme_prefix() str {
    readFileSync("README.md", "utf8")
    -o str.substring(_, 0, 5)
}

test "node builtin imports auto resolve" {
    assert str.equals(readme_prefix(), "# UTU")
}
